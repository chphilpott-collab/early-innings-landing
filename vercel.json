{
  "buildCommand": "echo 'Static site - no build needed'",
  "outputDirectory": "public",
  "public": true,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
